name: music_go_server
description: "Local game server - validates catches, stores collections, proxies music APIs."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.5.0
  http: ^1.2.0
  music_go_core:
    path: ../core
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_static: ^1.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
