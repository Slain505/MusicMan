name: music_go_core
description: Pure-Dart game core shared by the Flutter client and the server.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
